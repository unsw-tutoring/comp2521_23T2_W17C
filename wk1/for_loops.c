#include <stdio.h>

int main(void) {
	// rewrite this while loop as a for loop
	int j = 10;
	while (j >= 0) {
		printf("j is %d\n", j);
		j--;
	}
	printf("\n");

	// TODO: rewrite this while loop as a for loop
}
